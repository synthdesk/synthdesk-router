name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  call:
    uses: synthdesk/synthdesk-tools/.github/workflows/golden-gates-reusable.yml@ci-gates-v1.0.2
    with:
      repo_role: router
      python_router: "3.11"
    secrets: inherit
