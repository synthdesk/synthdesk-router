name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  call:
    uses: synthdesk/synthdesk/.github/workflows/golden-gates-reusable.yml@ci-gates-v1.0.1
    with:
      repo_role: router
      python_router: "3.11"
    secrets: inherit
