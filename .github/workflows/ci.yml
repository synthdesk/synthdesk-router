name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  call:
    uses: synthdesk/synthdesk-tools/.github/workflows/golden-gates-reusable.yml@aa08df69ffdd63adcedcc6b8740c87f8cf2972d5
    with:
      repo_role: router
      python_router: "3.11"
    secrets: inherit
