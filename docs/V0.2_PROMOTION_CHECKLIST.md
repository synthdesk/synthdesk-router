# V0.2 Authority Promotion Checklist

DOCTRINE: VETO_TIMESCALE
Created: 2026-01-23

## Pre-Promotion Verification

- [ ] Schema hash parity: `dad3e1d2824262be` (VPS = Local)
- [ ] Runtime enforcement active (boot assert passes)
- [ ] Surface streams populated (regime + micro)
- [ ] Soak period >= 24h complete

## Expected Behavior at v0.2

| Metric | Expected | Alert Threshold |
|--------|----------|-----------------|
| `router.decision.v1` volume | ≈ `router.intent` volume | Divergence > 5% |
| `system_veto_rate` | Near 0 (callers provide hold) | > 10% pauses promotion |
| `regime_block_rate` | Varies with market | Sudden spike > 3σ |
| `micro_annotate_rate` | High (NO_DATA expected) | N/A (informational) |

## Promotion Gate Questions

1. **Volume parity**: Do we see `router.decision.v1` ≈ intent volume?
2. **System veto rate**: What is the initial `system_veto_rate`?
   - If > 10%: caller integration debt, pause promotion
   - If ≈ 0%: readiness signal
3. **Alert threshold**: What triggers pause?
   - `system_veto_rate` spike = missing hold from callers
   - Schema hash mismatch = drift (impossible if assert holds)

## Post-Promotion Observation

Watch ONE metric: `system_veto_rate`

- Spike = integration debt (callers not providing intended_hold_min)
- Flat line ≈ 0 = readiness confirmed

## Do NOT Act On

- No inferred horizons
- No adaptive time defaults
- No "helpful" fallbacks
- No weakening the contract

Let friction surface reality.
